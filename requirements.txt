# Core
python-dotenv==1.0.1
pydantic==2.9.2

# Razorpay + agent orchestration
razorpay==1.4.2
langgraph==0.2.39
langchain-core==0.3.15

# Graph
neo4j==5.25.0
networkx==3.4.2

# ML — baseline + upgrade path
scikit-learn==1.5.2
joblib==1.4.2
xgboost==3.2.0  # bumped from 2.1.1 -- the real dev/demo machine had 3.2.0 installed
# (a full major version ahead of the old pin), which is what actually runs this
# project day to day. Re-pickled models/pattern_analyst*.joblib under 3.2.0 and
# verified bit-identical predict_proba on deterministic rows before/after --
# no retraining, just re-serialized to match the version that's really in use.
pandas==2.2.3
numpy>=1.26,<2.3  # pin loose on purpose: numpy 1.26.4 exact hits a known
# Windows/Python 3.13 longdouble OverflowError in numpy.core.getlimits when
# neo4j imports it; numpy>=2.0 also matches opencv-python's own floor if you
# have that installed. If you still hit the OverflowError, run:
#   pip install "numpy>=2.0,<2.3" --upgrade
# torch + torch-geometric: install separately (platform-specific wheels), see README
# Note: SHAP attribution in agents/verdict.py uses XGBoost's native
# pred_contribs, not the `shap` package -- the installed shap/xgboost
# version combo hit a known interop bug parsing XGBoost's serialized
# base_score, and pred_contribs gives identical values without it.

# Demo surface
streamlit==1.62.0
jinja2>=3.1.2  # required by pandas Styler (feed_df.style.apply in dashboard) -- pandas 2.3.x needs >=3.1.2, found via a real AppTest boot test that this wasn't pinned
fastapi==0.141.1
uvicorn[standard]==0.52.4
httpx==0.28.1  # needed by fastapi.testclient.TestClient

# Dev
pytest==8.3.3
